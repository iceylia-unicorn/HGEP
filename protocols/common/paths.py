from pathlib import Path

DATA_ROOT = Path("data")
ARTIFACT_ROOT = Path("artifacts")
CKPT_ROOT = ARTIFACT_ROOT / "checkpoints"
RESULT_ROOT = ARTIFACT_ROOT / "results"
LOG_ROOT = ARTIFACT_ROOT / "logs"